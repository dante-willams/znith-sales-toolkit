{
  "name": "znith-sales-toolkit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "engines": {
    "node": ">=20"
  }
}
